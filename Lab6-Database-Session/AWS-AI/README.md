# Test code only!
Calling a lambda function thats makes bedrock calls.

Put lambda and policies in place
```source create```

Edit explainer.html  
- Change the fetch URl to match your lambda URL.
- Optional: Modify the model ids that the dropdown allows.
- Push the html to a public location (S3).

Going away from the chess move context requires change to both the python and html.  

Take everything down
```soure remove```
